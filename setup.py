from setuptools import find_packages, setup

setup(
    name = 'plugml',
    version = '0.1.0',
    packages = find_packages(include = ['plugml']),
    description = 'establish connections to plugml',
    license='MIT',
    install_requires=[]
)